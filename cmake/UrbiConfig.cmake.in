@PACKAGE_INIT@

if (WIN32)
    get_filename_component(_IMPORT_PREFIX "${CMAKE_CURRENT_LIST_FILE}" PATH)
    get_filename_component(_IMPORT_PREFIX "${_IMPORT_PREFIX}" PATH)
    get_filename_component(_IMPORT_PREFIX "${_IMPORT_PREFIX}" PATH)
    get_filename_component(_IMPORT_PREFIX "${_IMPORT_PREFIX}" PATH)
    set_and_check(BOOSTROOT ${_IMPORT_PREFIX})
    set(_IMPORT_PREFIX)
endif()

include(CMakeFindDependencyMacro)

set(URBI_GIT_REVISION "@GIT_REVISION@")

find_dependency(Boost COMPONENTS regex system filesystem thread chrono date_time signals context)

include("${CMAKE_CURRENT_LIST_DIR}/UrbiTargets.cmake")
